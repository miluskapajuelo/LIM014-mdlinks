import {validate, mdLinks} from '../build/main.js'

describe('should validate link', () => {

  it('It should return from relative path to absolute path', () => {//preguntar
    expect(validate(RelativePath)).toBe(AbsolutePrueb);
  })})
